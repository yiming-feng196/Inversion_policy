# Behavior-region tracking legacy code

These scripts belong to an earlier exploration of behavior-region localization and temporal latent reuse. They are preserved for reference and are not used by the active Prior Flow pipeline.

The directory contains its own compatibility copy of `flow_latent_predictor_common.py` so historical scripts can run from this directory with the same MomentVLA environment.
